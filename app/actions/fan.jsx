export const fanRead = (data) => ({
  type: 'FAN_READ',
  data,
});

export const accelRead = (data) => ({
  type: 'ACCEL_READ',
  data,
});

export const clock = (data) => ({
  type: 'CLOCK',
  data,
});
